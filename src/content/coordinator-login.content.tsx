import { FC } from 'react';
import { CoordinatorLoginComponent } from '../components/coordinator-login/coordinator-login.component';

export const CoordinatorLoginContent: FC = (): JSX.Element => {
    return <CoordinatorLoginComponent/>
}