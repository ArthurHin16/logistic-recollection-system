import { FC } from 'react';
import { CoordinatorComponent } from '../components/coordinator/coordinator.component';

export const CoordinatorContent: FC = (): JSX.Element => {
    return <CoordinatorComponent/>
}