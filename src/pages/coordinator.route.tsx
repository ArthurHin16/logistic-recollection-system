import { FC } from 'react';
import { CoordinatorContent } from '../content/principal-coordinator.content';

export const CoordinatorRoute: FC = (): JSX.Element => {
    return <CoordinatorContent />
  }