import { FC } from 'react';
import { CoordinatorMapContent } from '../content/coordinator-map.content';

export const CoordinatorMapRoute: FC = (): JSX.Element => {
    return <CoordinatorMapContent/>
}