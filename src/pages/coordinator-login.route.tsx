import { FC } from 'react';
import { CoordinatorLoginContent } from '../content/coordinator-login.content';

export const CoordinatorLoginRoute: FC = (): JSX.Element => {
    return <CoordinatorLoginContent/>
}